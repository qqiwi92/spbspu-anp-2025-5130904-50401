#ifndef VARIANTS_HPP
#define VARIANTS_HPP

#include <cstddef>

namespace chernov {
  void fllIncWav(int * mtx, size_t rows, size_t cols);
  int minSumMdg(const int * mtx, size_t rows, size_t cols);
}

#endif
